<h1>My lovely twig template.</h1>
<p>You passed this text: <b>{{ variable1 }}</b></p>

  <p>And you passed this array:</p>
  <ul>
    {% for item in variable2 %}
      {% if item is iterable %}
        <li>
          Route {{ loop.index }}: <a href="{{ path(item[0], { 'count': item[1] }) }}">{{ item[0] }}</a>
        </li>
      {% else %}
        <li>
          Route {{ loop.index }}: <a href="{{ path(item) }}">{{ item }}</a>
        </li>
      {% endif %}
    {% endfor %}
  </ul>

<p>Ciao!</p>
